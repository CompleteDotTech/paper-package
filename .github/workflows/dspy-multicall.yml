name: Live DSPy multi-call transfer benchmarks
# Requires a successful frozen upstream capture; manual until that run is pinned.
on:
  workflow_dispatch:
permissions:
  contents: read
  actions: read
jobs:
  prerequisite:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'A successful primary capture must be pinned before transfer inference.' && exit 1
