# Source-aware structural extension: author-side verification

This is an AI-assisted author-side review, not independent scientific peer review. The research remains opt-in and changes no production graph policy.

## Executed and reproduced evidence

The protocol anchor is `b92009c6c36d87d9f3cbf0c92c2dad9f9615d726`, initially based on PR #17 (`a62a3257645d8e35cd4e45be53bfa9511d27724b`). Concurrent PR #18 main (`7e2ce2fce28f29ebc6a2b462d063ea558b656cc1`) is a genuine second parent of integration commit `654dfe0c00cde875c075b541932a51cb052440d8`. Its complete structural package, protocol and numerical evidence are preserved. Publication integration does not retune any hypothesis, threshold, fit or outcome.

The integrated execution/build [run 35327832466](https://github.com/CompleteDotTech/paper-package/actions/runs/35327832466) completed successfully and committed its artifacts as `df9ba9ff8963d2917e4d6d457acbd4320e253f58`. The downloaded exact-source artifact was inspected separately in the local runtime. All five extension manifests (adaptive, risk-control, reliability, structural and source-structural) verify against their committed files. The current manuscript, renderer and PDF hashes also verify.

The [combined validation record](source_structural/combined-validation.json) and [captured log](source_structural/combined-tests.log) contain **651 passing tests**: 200 core, 32 corpus, 37 theory, 67 follow-up, 64 adaptive, 66 risk-control, 63 reliability, 36 concurrent structural and 86 source-structural. The same combined suite passed locally. The latter 86 include two concurrent-publication tests: regenerating either extension preserves both sections and remains idempotent.

The original study audit records **161 archive files verified and zero original files changed**. Initial local results and integrated GitHub results are byte-identical, SHA-256 `073c7b1f43a1b8f843cd4a936b2bf1ec440e1b9d2db64180b3a127d054d1fb13`. The full integrated paper has **79 pages**; its PDF SHA-256 is `be2e87ffa453fbdc2504edd20e0e287dd65d80294c455285e31e9cc3bdad1e25`. Automated PDF inspection found no text bounding boxes outside page boundaries. The added section's rendered tables and figures were inspected for layout and readability.

## Scientific and implementation audit

H1 and H2 fail their frozen targets and remain explicitly unfavorable. A direct group-event estimator does not improve Brier or meet the bias threshold. An exact budget optimizer reviews more edges but leaves more label-evaluated expected contamination under the supplied review assumptions. These results do not justify deployment.

H3-H5 pass controlled algorithmic targets, not independent semantic targets. Frontier inference uses supplied independent primitives; false provenance remains a failure. A valid bipartite flow/cover certificate proves the supplied priority objective, not assertion truth. Revision-checked cache maintenance is sequential and in-memory; its 98.83% local reduction counts fact reevaluations, not total CPU time. A global-source control saves zero evaluations.

All runs make **zero fresh Jev calls**. H1 reuses archived predictions; H2 is a reviewer simulation, including hypothetical setup costs per connected source group rather than actual document-opening costs; H3-H5 use controlled fixtures. Reused evaluation labels, small calibration samples, fixed-fit descriptive intervals, assumed reviewer behavior and supplied priorities/probabilities limit interpretation. The concurrent studies cannot be pooled as independent semantic observations. No advantage over KARMA or another external system is established.

## Merge gate

The final PR head must pass the repository workflows, including Linux/Windows offline replay and deterministic full-manuscript/figure regeneration. This document records completed execution and inspection, not a claim that later commit checks have already finished. The PR checks and merge record identify the final checked head. Use a normal merge commit to retain both frozen protocols and the concurrent integration in ancestry.
