# Pre-merge implementation review

The protocol and primary thresholds remain unchanged. Review found that the initial H1 bookkeeping used the ratio of total correct-edge counts for retention, which could let newly recovered edges mask loss of baseline-correct edges. Retention now compares exact candidate IDs and labels: the intersection of baseline-correct and proposed-correct edges divided by the baseline-correct count. A regression test explicitly adds two new correct edges while losing one old correct edge and requires retention 1/2, not the misleading count ratio 3/2. On the recorded panel, all 132 baseline-correct edges survive; retention is 100%. The matched-volume error-reduction failure is unchanged.

The first clean-runner workflow executed all five benchmarks, passed 64 new and 529 existing tests, and reproduced results with no numeric differences. Its publication phase was blocked because the original-inventory verifier refuses an output path inside the repository. The workflow now writes that audit outside the repository and copies the completed audit into this additive extension. No original inventory file or verification rule was altered.

The dependence chart was reviewed to display an interval without a midpoint marker that could imply a point estimate. Dense conflict-check behavior, the priority-greedy tie on large bipartite fixtures, invalid-marginal and false-priority controls, and the failed semantic-adaptation target remain visible. Canonical runner results, figures and full paper are generated by the workflow; no manually fabricated result files are uploaded.


## Final cross-workflow publication audit

The final integrated PR run passed algorithmic checks, but the separately maintained structural-frontier figure/paper job rebuilt its own section without reapplying these additive sections. Its paper diff therefore removed or reordered this extension rather than exposing a benchmark mismatch. Two explicit report-generation commands are now inserted before that existing paper-diff gate. The gate, original figure checks, renderer invocation and all other workflow content remain unchanged. The publication integration audit verifies this exact transformation against main; regression tests require idempotence, preservation of every non-added line and failure on an unrecognized layout. No scientific protocol, primary threshold, benchmark implementation or input is changed by this correction.
